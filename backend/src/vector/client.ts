export async function embedText(text: string, sessionId: string) {
  // Dummy embedding logic. Replace with OpenAI embeddings + vector DB integration.
  console.log(`Embedding text for session ${sessionId}: ${text.substring(0, 30)}…`);
}
